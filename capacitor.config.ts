import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'es.josesaranda.jcofrade',
  appName: 'jCofrade',
  webDir: 'www',
  bundledWebRuntime: false
};

export default config;
